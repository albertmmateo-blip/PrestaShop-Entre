# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF line endings (critical for Docker containers)
*.sh text eol=lf

# Dockerfile must always use LF line endings
Dockerfile text eol=lf
